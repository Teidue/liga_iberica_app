# Configuración pg_hba.conf para desarrollo
# Conexiones locales - trust
local   all             all                                     trust

# IPv4 - trust para desarrollo
host    all             all             127.0.0.1/32            trust
host    all             all             0.0.0.0/0               trust

# IPv6 - trust para desarrollo
host    all             all             ::1/128                 trust
host    all             all             ::/0                    trust
